{
  "name": "the-last-known-podcast-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "WRANGLER_WRITE_LOGS=false wrangler dev",
    "deploy": "WRANGLER_WRITE_LOGS=false wrangler deploy",
    "check": "WRANGLER_WRITE_LOGS=false wrangler deploy --dry-run",
    "migrate:youtube-videos": "node scripts/migrate_youtube_videos_to_r2.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
